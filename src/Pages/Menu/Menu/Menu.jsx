import { Helmet } from "react-helmet";

const Menu = () => {
  return (
    <div>
      <Helmet>
        <title>Quick Grab | Menu</title>
      </Helmet>
      <h2>this is menu page</h2>
    </div>
  );
};

export default Menu;
